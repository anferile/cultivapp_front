// API stub — todas las llamadas al backend han sido eliminadas en esta versión.
// El flujo de agendamiento usa WhatsApp directamente (sin backend).
// Este archivo se mantiene por compatibilidad; no exporta nada relevante.
export {}
